# Example seem_layout_plugin plugin definitions.
# Plugin property $defaults are defined in SeemLayoutPluginManager.
label: 'Two'
regions:
  first:
    -
      type: markup
      markup: 'ASDASDIch bin Markup über dem Content'
    -
      type: main_content
    -
      type: content_block
      bid: 1
  second:
      -
        type: markup
        markup: 'ASDASDIch bin eine Sidebar, Frei definiertes Markup über das yaml'
