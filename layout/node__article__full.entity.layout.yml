# Example seem_layout_plugin plugin definitions.
# Plugin property $defaults are defined in SeemLayoutPluginManager.
id: one
label: 'One'
regions:
  first:
    -
      type: markup
      markup: 'Ich bin Markup über dem Content'
    -
      type: context
  second:
      -
        type: markup
        markup: 'Ich bin eine Sidebar, Frei definiertes Markup über das yaml'
